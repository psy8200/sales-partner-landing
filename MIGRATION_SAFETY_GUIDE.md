# 🛡️ 마이그레이션 안전 가이드

## ⚠️ 중요: 데이터 손실 방지를 위한 필수 절차

### 📋 마이그레이션 실행 전 체크리스트

#### 1. 백업 생성 (필수)
```bash
# 마이그레이션 실행 전 반드시 백업 생성
node scripts/backup-before-migration.js
```

#### 2. 현재 데이터 상태 확인
- 사용자 수
- 계약 수  
- 아이템 수
- 문의 수

#### 3. 마이그레이션 실행
```bash
# 백업 완료 후에만 마이그레이션 실행
npx prisma migrate dev --name your_migration_name
```

#### 4. 마이그레이션 후 검증
- 데이터 무결성 확인
- 애플리케이션 정상 동작 확인
- API 응답 확인

### 🚨 데이터 손실 사고 방지

#### 과거 사고 사례
- **2025년 9월 8일 오후 5:14**: `20250908081406_add_sidebar_items` 마이그레이션 실행 시 User 테이블 재정의 과정에서 10명의 회원 데이터 삭제

#### 사고 원인
- 마이그레이션에서 테이블 구조 변경 시 데이터 복사 과정에서 오류 발생
- 백업 없이 마이그레이션 실행

### 🔧 복구 절차

#### 백업에서 복구
```bash
# 사용 가능한 백업 파일 확인
node scripts/restore-from-backup.js

# 특정 백업 파일로 복구
node scripts/restore-from-backup.js dev-backup-before-migration-2025-09-08T10-30-00-000Z.db
```

### 📝 안전한 마이그레이션 워크플로우

1. **백업 생성**
   ```bash
   node scripts/backup-before-migration.js
   ```

2. **마이그레이션 실행**
   ```bash
   npx prisma migrate dev --name your_migration_name
   ```

3. **데이터 검증**
   - 어드민 패널에서 사용자 수 확인
   - 계약 데이터 확인
   - 아이템 데이터 확인

4. **애플리케이션 테스트**
   - 로그인/로그아웃 테스트
   - 주요 기능 테스트
   - API 응답 확인

### 🚫 절대 하지 말 것

- ❌ 백업 없이 마이그레이션 실행
- ❌ 테이블 구조 변경 시 데이터 검증 생략
- ❌ 마이그레이션 후 데이터 확인 생략
- ❌ 프로덕션 환경에서 직접 마이그레이션 실행

### ✅ 반드시 할 것

- ✅ 마이그레이션 전 백업 생성
- ✅ 마이그레이션 후 데이터 검증
- ✅ 애플리케이션 기능 테스트
- ✅ 문제 발생 시 즉시 백업으로 복구

### 📞 긴급 상황 대응

#### 데이터 손실 발견 시
1. 즉시 서버 중지
2. 백업 파일 확인
3. 백업에서 복구
4. 원인 분석 및 재발 방지

#### 복구 명령어
```bash
# 서버 중지
taskkill /IM node.exe /F

# 백업에서 복구
node scripts/restore-from-backup.js [백업파일명]

# 서버 재시작
npm run dev
```

---

**⚠️ 이 가이드를 반드시 준수하여 데이터 손실을 방지하세요!**


